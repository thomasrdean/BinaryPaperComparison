sudo docker build -t antlr_dns .
sudo docker run -it -v ~/msc/DNSpcaps:/DNSpcaps antlr_dns /antlr/binary/build/parser /DNSpcaps/dns_all_rr_types_udp.pcap

for dev:
sudo docker run -it -v ~/msc/DNSpcaps:/DNSpcaps -v "$(pwd)"/binary/grammars:/antlr/binary/grammars antlr_dns /bin/bash
edit grammars/DNS.g4 locally
make
apt install gdb
gdb parser
run /DNSpcaps/dns_all_rr_types_udp.pcap

sudo docker run -it -v ~/msc/DNSpcaps:/DNSpcaps antlr_dns -v binary/grammars/DNS.g4:/antlr/binary/grammars/ --name antlr_dns_container /antlr/binary/build/parser /DNSpcaps/dns_all_rr_types_udp.pcap
sudo docker exec -it antlr_dns_container /bin/bash
sudo docker container stop antlr_dns_container
